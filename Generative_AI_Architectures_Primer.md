# Generative AI Architectures: A Comprehensive Primer

---

## Table of Contents

1. [Transformer Architecture](#1-transformer-architecture)
2. [Diffusion Models](#2-diffusion-models)
3. [Generative Adversarial Networks (GANs)](#3-generative-adversarial-networks-gans)
4. [Variational Autoencoders (VAEs)](#4-variational-autoencoders-vaes)
5. [Multimodal Vision-Language Models](#5-multimodal-vision-language-models)
6. [Hybrid and Emerging Architectures](#6-hybrid-and-emerging-architectures)
7. [Architecture Comparison](#7-architecture-comparison)

---

## 1. Transformer Architecture

### Overview

The Transformer was proposed in 2017 by Google researchers in the landmark paper **"Attention Is All You Need"**. It replaced recurrence with **self-attention**, allowing efficient parallelization, longer context handling, and scalable training on unprecedented data volumes. This innovation enabled models like GPT, BERT, and their successors, which demonstrated emergent behaviors at scale, such as few-shot learning and compositional reasoning.

### Core Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   TRANSFORMER ARCHITECTURE                   │
├─────────────────────────┬───────────────────────────────────┤
│        ENCODER          │           DECODER                 │
│                         │                                   │
│  ┌───────────────────┐  │  ┌───────────────────┐           │
│  │   Output Vectors   │  │  │   Output Probs    │           │
│  └────────┬──────────┘  │  └────────┬──────────┘           │
│           │              │           │                       │
│  ┌────────▼──────────┐  │  ┌────────▼──────────┐           │
│  │  Feed-Forward NN  │  │  │  Feed-Forward NN  │           │
│  │  + Layer Norm     │  │  │  + Layer Norm     │           │
│  └────────┬──────────┘  │  └────────┬──────────┘           │
│           │              │           │                       │
│  ┌────────▼──────────┐  │  ┌────────▼──────────┐           │
│  │  Multi-Head       │  │  │  Cross-Attention  │◄──── From │
│  │  Self-Attention   │  │  │  (Encoder-Decoder) │     Encoder│
│  │  + Layer Norm     │  │  │  + Layer Norm     │           │
│  └────────┬──────────┘  │  └────────┬──────────┘           │
│           │              │           │                       │
│           │              │  ┌────────▼──────────┐           │
│           │              │  │  Masked Multi-Head│           │
│           │              │  │  Self-Attention   │           │
│           │              │  │  + Layer Norm     │           │
│           │              │  └────────┬──────────┘           │
│           │              │           │                       │
│  ┌────────▼──────────┐  │  ┌────────▼──────────┐           │
│  │  Positional       │  │  │  Positional       │           │
│  │  Encoding         │  │  │  Encoding         │           │
│  │  + Input Embed    │  │  │  + Output Embed   │           │
│  └────────┬──────────┘  │  └────────┬──────────┘           │
│           │              │           │                       │
│      [Input Tokens]      │     [Output Tokens]              │
└─────────────────────────┴───────────────────────────────────┘
```

### Self-Attention Mechanism

```
                    ┌───────────────────────┐
                    │    Input Embeddings    │
                    └───────┬───────────────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
         ┌────────┐   ┌────────┐   ┌────────┐
         │ W_Q    │   │ W_K    │   │ W_V    │
         │(Query) │   │ (Key)  │   │(Value) │
         └───┬────┘   └───┬────┘   └───┬────┘
             ▼             ▼             │
        ┌─────────────────────┐          │
        │   Q × K^T / √d_k   │          │
        │  (Scaled Dot-Product│          │
        │     Attention)      │          │
        └──────────┬──────────┘          │
                   ▼                     │
            ┌─────────────┐              │
            │   Softmax   │              │
            └──────┬──────┘              │
                   │                     │
                   ▼                     ▼
              ┌────────────────────────────┐
              │  Attention Weights × V     │
              └────────────┬───────────────┘
                           ▼
                  ┌─────────────────┐
                  │  Context Vector  │
                  └─────────────────┘
```

### Key Variants

#### Encoder-Only: BERT

```
       [CLS] Token1 Token2 ... TokenN [SEP]
           │      │      │          │
           ▼      ▼      ▼          ▼
     ┌──────────────────────────────────┐
     │   Token + Segment + Position     │
     │          Embeddings              │
     └──────────────┬───────────────────┘
                    ▼
     ┌──────────────────────────────────┐
     │    Bidirectional Self-Attention  │
     │    Encoder Block × 12 (base)    │  ◄── Sees ALL tokens
     │          or × 24 (large)        │      in BOTH directions
     └──────────────┬───────────────────┘
                    ▼
     ┌──────────────────────────────────┐
     │   Contextual Representations     │
     └──────┬───────┬──────────┬────────┘
            ▼       ▼          ▼
     ┌──────────┐ ┌─────┐ ┌────────────┐
     │Classific.│ │ NER │ │   Q&A      │
     │   Head   │ │Head │ │   Head     │
     └──────────┘ └─────┘ └────────────┘

  Pre-training Tasks:
  • Masked Language Modeling (MLM): Predict [MASK] tokens
  • Next Sentence Prediction (NSP)
```

#### Decoder-Only: GPT

```
      Token1  Token2  Token3  ...  TokenN
        │       │       │            │
        ▼       ▼       ▼            ▼
  ┌──────────────────────────────────────┐
  │   Token + Position Embeddings        │
  └──────────────┬───────────────────────┘
                 ▼
  ┌──────────────────────────────────────┐
  │   Masked (Causal) Self-Attention     │
  │   Decoder Block × 96 (GPT-3)        │  ◄── Each token can
  │   175 Billion Parameters             │      only attend to
  └──────────────┬───────────────────────┘      PREVIOUS tokens
                 ▼
  ┌──────────────────────────────────────┐
  │   Next Token Prediction              │
  │   P(token_n+1 | token_1...token_n)   │
  └──────────────────────────────────────┘

  Left-to-right autoregressive generation:
  "The cat" → "sat" → "on" → "the" → "mat"
```

### Use Cases

- **NLP:** Text generation, translation, summarization, Q&A
- **Code Generation:** GitHub Copilot, code completion
- **Vision:** Vision Transformers (ViT) for image classification
- **Multimodal:** GPT-4V, Gemini (text + image reasoning)
- **Robotics & RL:** Decision Transformers

---

## 2. Diffusion Models

### Overview

Diffusion models are a class of latent variable generative models consisting of two major components: the **forward diffusion process** (adding noise) and the **reverse sampling process** (removing noise). The model learns to reverse the process of adding noise to generate high-quality outputs in domains such as image generation, text-to-image translation, audio synthesis, and molecular design.

### Core Architecture: Forward and Reverse Process

```
  FORWARD PROCESS (Training — Adding Noise)
  ══════════════════════════════════════════

  x₀ ──────► x₁ ──────► x₂ ──────► ... ──────► x_T
  (clean)   (+noise)   (+noise)                (pure noise)

  ┌──────┐  ┌──────┐  ┌──────┐            ┌──────────┐
  │🖼️    │→ │🖼️+ε │→ │🖼️+εε│→  ...  →  │ ░░░░░░░░ │
  │image │  │      │  │      │            │ Gaussian  │
  │      │  │      │  │      │            │  Noise    │
  └──────┘  └──────┘  └──────┘            └──────────┘
    t=0       t=1       t=2        ...        t=T


  REVERSE PROCESS (Inference — Removing Noise)
  ══════════════════════════════════════════════

  x_T ──────► x_{T-1} ──────► ... ──────► x₁ ──────► x₀
  (noise)    (denoise)                   (denoise)   (clean!)

  ┌──────────┐  ┌──────┐            ┌──────┐  ┌──────┐
  │ ░░░░░░░░ │→ │🖼️-ε │→  ...  →  │🖼️-ε │→ │🖼️    │
  │ Gaussian  │  │      │            │      │  │result│
  │  Noise    │  │      │            │      │  │image │
  └──────────┘  └──────┘            └──────┘  └──────┘
     t=T         t=T-1       ...      t=1       t=0
                    ▲
                    │
            ┌───────┴────────┐
            │  Learned U-Net │
            │  (noise pred.) │
            └────────────────┘
```

### Latent Diffusion Model (Stable Diffusion)

```
┌─────────────────────────────────────────────────────────────────────┐
│                    LATENT DIFFUSION MODEL                           │
│                                                                     │
│  ┌──────────┐    ┌──────────────────────────────────┐  ┌────────┐  │
│  │          │    │     LATENT SPACE (compressed)     │  │        │  │
│  │   VAE    │    │                                   │  │  VAE   │  │
│  │ Encoder  │───►│  ┌────┐  ┌────┐      ┌────┐     │──►│Decoder │  │
│  │          │    │  │z_T │→ │z_..│ → →  │z_0 │     │  │        │  │
│  │ (Image → │    │  │noise│  │    │      │clean│    │  │(Latent │  │
│  │  Latent) │    │  └────┘  └──▲─┘      └────┘     │  │→ Image)│  │
│  │          │    │             │                      │  │        │  │
│  └──────────┘    │     ┌───────┴────────┐            │  └────────┘  │
│                  │     │     U-Net      │            │              │
│                  │     │  (Denoiser)    │            │              │
│                  │     │               │            │              │
│                  │     │  ┌──────────┐ │            │              │
│                  │     │  │Cross-Attn│◄┼────────┐   │              │
│                  │     │  └──────────┘ │        │   │              │
│                  │     └───────────────┘        │   │              │
│                  └──────────────────────────────┼───┘              │
│                                                 │                  │
│                                          ┌──────┴───────┐          │
│                                          │ Text Encoder │          │
│                                          │   (CLIP)     │          │
│                                          └──────┬───────┘          │
│                                                 │                  │
│                                          ┌──────┴───────┐          │
│                                          │ "A cat on a  │          │
│                                          │  beach at    │          │
│                                          │  sunset"     │          │
│                                          └──────────────┘          │
└─────────────────────────────────────────────────────────────────────┘
```

### U-Net Architecture (Denoiser)

```
          Input (noisy latent)
                │
     ┌──────────▼──────────┐
     │    Conv + Attn      │──────────────────────┐
     │    (64 channels)    │                      │ Skip
     └──────────┬──────────┘                      │ Connection
                │ Downsample                      │
     ┌──────────▼──────────┐                      │
     │    Conv + Attn      │─────────────────┐    │
     │    (128 channels)   │                 │    │
     └──────────┬──────────┘                 │    │
                │ Downsample                 │    │
     ┌──────────▼──────────┐                 │    │
     │    Conv + Attn      │────────────┐    │    │
     │    (256 channels)   │            │    │    │
     └──────────┬──────────┘            │    │    │
                │ Downsample            │    │    │
     ┌──────────▼──────────┐            │    │    │
     │     BOTTLENECK      │            │    │    │
     │   (512 channels)    │            │    │    │
     └──────────┬──────────┘            │    │    │
                │ Upsample              │    │    │
     ┌──────────▼──────────┐            │    │    │
     │  Conv + Attn + Cat  │◄───────────┘    │    │
     │    (256 channels)   │                 │    │
     └──────────┬──────────┘                 │    │
                │ Upsample                   │    │
     ┌──────────▼──────────┐                 │    │
     │  Conv + Attn + Cat  │◄────────────────┘    │
     │    (128 channels)   │                      │
     └──────────┬──────────┘                      │
                │ Upsample                        │
     ┌──────────▼──────────┐                      │
     │  Conv + Attn + Cat  │◄─────────────────────┘
     │    (64 channels)    │
     └──────────┬──────────┘
                │
                ▼
       Predicted Noise (ε)
```

### Key Variants

| Variant | Description | Example Models |
|---|---|---|
| DDPM | Standard denoising diffusion in pixel space | Original Ho et al. 2020 |
| Latent Diffusion | Diffusion in compressed latent space via VAE | Stable Diffusion, DALL-E 2 |
| Diffusion Transformer (DiT) | Replaces U-Net with Transformer backbone | Stable Diffusion 3, Sora |
| Cascaded Diffusion | Multiple sub-models at increasing resolutions | Imagen |
| Score-based (SGM) | Uses score matching + Langevin dynamics | Song & Ermon 2019 |

### Use Cases

- **Image Generation:** Stable Diffusion, DALL-E 2/3, Midjourney
- **Video Generation:** Sora, Runway Gen-3, Make-A-Video
- **Image Editing:** Inpainting, outpainting, super-resolution
- **Audio Synthesis:** Music and speech generation
- **Molecular Design:** Drug discovery and protein structure
- **3D Generation:** Stable Video 4D, 3D object synthesis

---

## 3. Generative Adversarial Networks (GANs)

### Overview

Introduced by Ian Goodfellow in **2014**, GANs consist of two neural networks — a **generator** and a **discriminator** — trained simultaneously in an adversarial competition. The generator produces new samples while the discriminator aims to distinguish between fake and real data. By incorporating this adversarial competition, GANs can produce remarkably realistic and diverse image and video samples.

### Core Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     GAN ARCHITECTURE                         │
│                                                              │
│                                                              │
│  ┌──────────┐      ┌─────────────┐      ┌──────────────┐   │
│  │  Random   │      │             │      │              │   │
│  │  Noise    │─────►│  GENERATOR  │─────►│  Generated   │   │
│  │  Vector   │      │     (G)     │      │  Image       │   │
│  │  (z~N)    │      │             │      │  G(z)        │   │
│  └──────────┘      └─────────────┘      └──────┬───────┘   │
│                                                  │           │
│                                                  ▼           │
│                                          ┌──────────────┐   │
│                                          │              │   │
│  ┌──────────────┐                        │DISCRIMINATOR │   │
│  │  Real        │───────────────────────►│     (D)      │   │
│  │  Images      │                        │              │   │
│  │  (training)  │                        │  Real or     │   │
│  └──────────────┘                        │  Fake?       │   │
│                                          └──────┬───────┘   │
│                                                  │           │
│                                                  ▼           │
│                                          ┌──────────────┐   │
│                                          │  D(x) → 1   │   │
│                                          │  (Real)      │   │
│                                          │  D(G(z)) → 0 │   │
│                                          │  (Fake)      │   │
│                                          └──────────────┘   │
│                                                              │
│  ◄──────────── Feedback Loop (Backpropagation) ──────────►  │
│  G tries to MAXIMIZE D's error | D tries to MINIMIZE error  │
└─────────────────────────────────────────────────────────────┘
```

### GAN Training Flow

```
  ┌──────────────────────────────────────────────────────┐
  │                GAN TRAINING LOOP                      │
  │                                                       │
  │  Step 1: Train Discriminator                          │
  │  ┌───────┐   ┌───┐   ┌───┐                          │
  │  │Real(x)│──►│ D │──►│ 1 │  (label: real)           │
  │  └───────┘   └───┘   └───┘                          │
  │  ┌───────┐   ┌───┐   ┌───┐   ┌───┐                  │
  │  │Noise z│──►│ G │──►│ D │──►│ 0 │  (label: fake)   │
  │  └───────┘   └───┘   └───┘   └───┘                  │
  │  Update D weights only ✓                              │
  │                                                       │
  │  Step 2: Train Generator                              │
  │  ┌───────┐   ┌───┐   ┌───┐                          │
  │  │Noise z│──►│ G │──►│ D │──► wants D to say "1"    │
  │  └───────┘   └───┘   └───┘   (fool the discriminator)│
  │  Update G weights only ✓                              │
  │                                                       │
  │  Repeat until equilibrium (Nash Equilibrium)          │
  └──────────────────────────────────────────────────────┘
```

### Notable GAN Variants

```
  GAN Evolution Timeline:
  ══════════════════════════════════════════════════════════

  2014          2015          2016          2017       2018+
   │             │             │             │          │
   ▼             ▼             ▼             ▼          ▼
  GAN ───► DCGAN ───► Pix2Pix ───► WGAN ───► StyleGAN
  (Original)  (Deep       (Image-to-  (Wasserstein (High-res
              Convol.)    Image)       distance)   face gen.)

                                              ───► CycleGAN
                                              (Unpaired image
                                               translation)

                                              ───► ProGAN
                                              (Progressive
                                               growing)

                                              ───► BigGAN
                                              (Large-scale
                                               class-conditional)
```

### Strengths & Challenges

| Strengths | Challenges |
|---|---|
| Sharp, high-fidelity outputs | Training instability |
| Excellent for realistic images | Mode collapse |
| Fast inference (single pass) | Hard to find equilibrium |
| Good for style transfer | No explicit density estimation |

### Use Cases

- **Image Synthesis:** Photorealistic face generation (StyleGAN)
- **Image-to-Image Translation:** Style transfer, colorization
- **Super Resolution:** Enhancing low-resolution images
- **Data Augmentation:** Generating synthetic training data
- **Video Generation:** Face swap, deepfake technology
- **Medical Imaging:** Synthetic scans for training

---

## 4. Variational Autoencoders (VAEs)

### Overview

Variational Autoencoders were introduced in **2014** by Diederik P. Kingma and Max Welling. VAEs are generative models explicitly designed to capture the underlying probability distribution of a given dataset and generate novel samples. They consist of two major components: an **encoder** that maps data to a latent distribution, and a **decoder** that reconstructs data from sampled latent vectors.

### Core Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    VAE ARCHITECTURE                               │
│                                                                   │
│       INPUT                 LATENT SPACE              OUTPUT      │
│                                                                   │
│  ┌──────────┐    ┌──────────────────────┐    ┌──────────────┐   │
│  │          │    │                      │    │              │   │
│  │  Input   │    │    μ (mean)          │    │  Reconstructed│  │
│  │  Image   │──►│──►──────┐            │──►│  Image        │   │
│  │  (x)     │    │        ▼            │    │  (x̂)         │   │
│  │          │    │    ┌────────┐       │    │              │   │
│  └──────────┘    │    │ Sample │       │    └──────────────┘   │
│       │          │    │z=μ+σ·ε │       │          ▲            │
│       ▼          │    └────────┘       │          │            │
│  ┌──────────┐    │        ▲            │    ┌──────────┐      │
│  │          │    │    σ (std dev)       │    │          │      │
│  │ ENCODER  │    │                      │    │ DECODER  │      │
│  │ q(z|x)   │    │   ε ~ N(0, I)       │    │ p(x|z)   │      │
│  │          │    │   (random noise)     │    │          │      │
│  └──────────┘    └──────────────────────┘    └──────────┘      │
│                                                                   │
│  Loss = Reconstruction Loss + KL Divergence                      │
│       = ||x - x̂||² + KL(q(z|x) || p(z))                        │
└─────────────────────────────────────────────────────────────────┘
```

### VAE Data Flow

```
                      ENCODING
  ┌───────────┐    ┌───────────┐    ┌─────────────────┐
  │   Input   │    │  Neural   │    │ Distribution     │
  │   Data    │───►│  Network  │───►│ Parameters       │
  │   (x)     │    │ (Encoder) │    │ μ = [0.2, -1.5] │
  └───────────┘    └───────────┘    │ σ = [0.3,  0.8] │
                                     └────────┬────────┘
                                              │
                                    Reparameterization
                                     z = μ + σ · ε
                                              │
                                              ▼
                      DECODING       ┌─────────────┐
  ┌───────────┐    ┌───────────┐    │  Latent      │
  │ Generated │◄───│  Neural   │◄───│  Vector (z)  │
  │   Data    │    │  Network  │    │  [0.5, -0.3] │
  │   (x̂)    │    │ (Decoder) │    └─────────────┘
  └───────────┘    └───────────┘

  Latent Space Visualization:
  ┌─────────────────────────────────┐
  │           •  •   •              │
  │        •   CATS  •  •           │
  │      •  •  •  •   •            │
  │                                 │  ◄── Smooth, continuous
  │          •  •                   │      latent space allows
  │       •  DOGS  •  •            │      interpolation between
  │     •  •  •  •   •             │      concepts
  │                                 │
  └─────────────────────────────────┘
```

### Strengths & Challenges

| Strengths | Challenges |
|---|---|
| Stable training | Blurrier outputs than GANs |
| Smooth, interpretable latent space | Limited sharpness |
| Good for interpolation | Trade-off: reconstruction vs. regularization |
| Probabilistic framework | Can miss fine details |
| Diverse outputs | |

### Use Cases

- **Image Generation:** Novel image synthesis
- **Anomaly Detection:** Fraud detection, medical diagnosis (tumors, diseases)
- **Data Compression:** Dimensionality reduction
- **Drug Discovery:** Molecular generation
- **Signal Analysis:** Stock market patterns, health monitoring
- **Content Generation:** 3D models from 2D images, handwritten text
- **Key Role in Diffusion Models:** VAE encoder/decoder used in Stable Diffusion's latent space

---

## 5. Multimodal Vision-Language Models

### Overview

Vision Language Models (VLMs) bridge the gap between visual and linguistic understanding of AI. They consist of a multimodal architecture that learns to associate information from image and text modalities. By using advances in Transformers and pretraining strategies, VLMs unlock the potential of vision-language applications ranging from image search to image captioning, generative tasks, and more.

### General VLM Architecture

```
┌─────────────────────────────────────────────────────────────┐
│              VISION-LANGUAGE MODEL (General)                  │
│                                                              │
│  ┌───────────┐                     ┌───────────┐            │
│  │           │                     │           │            │
│  │   Image   │                     │   Text    │            │
│  │   Input   │                     │   Input   │            │
│  │           │                     │           │            │
│  └─────┬─────┘                     └─────┬─────┘            │
│        │                                 │                   │
│        ▼                                 ▼                   │
│  ┌───────────┐                     ┌───────────┐            │
│  │  Vision   │                     │   Text    │            │
│  │  Encoder  │                     │  Encoder  │            │
│  │  (ViT /   │                     │  (BERT /  │            │
│  │  ResNet)  │                     │  GPT)     │            │
│  └─────┬─────┘                     └─────┬─────┘            │
│        │                                 │                   │
│        │    ┌────────────────────┐        │                   │
│        └───►│   Multimodal      │◄───────┘                   │
│             │   Fusion Module   │                            │
│             │  (Cross-Attention/│                            │
│             │   Dot Product/    │                            │
│             │   Co-Attention)   │                            │
│             └────────┬──────────┘                            │
│                      │                                       │
│                      ▼                                       │
│             ┌────────────────────┐                           │
│             │  Task-Specific     │                           │
│             │  Output Head /     │                           │
│             │  LLM Decoder       │                           │
│             └────────┬──────────┘                            │
│                      │                                       │
│                      ▼                                       │
│         ┌──────────────────────────┐                        │
│         │ Caption / Answer / Label  │                        │
│         └──────────────────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

### CLIP Architecture (Contrastive Learning)

```
┌─────────────────────────────────────────────────────────────┐
│            CLIP: Contrastive Language-Image Pre-training      │
│                                                              │
│   IMAGE BATCH                        TEXT BATCH              │
│  ┌────────┐                        ┌──────────────┐         │
│  │ Dog 🐕 │                        │ "A photo of  │         │
│  │ Cat 🐈 │                        │  a dog"      │         │
│  │ Car 🚗 │                        │ "A cute cat" │         │
│  │  ...   │                        │ "Red car"    │         │
│  └───┬────┘                        └──────┬───────┘         │
│      │                                     │                 │
│      ▼                                     ▼                 │
│  ┌────────┐                        ┌───────────┐            │
│  │ Image  │                        │   Text    │            │
│  │Encoder │                        │  Encoder  │            │
│  │ (ViT)  │                        │(Transf.)  │            │
│  └───┬────┘                        └─────┬─────┘            │
│      │                                    │                  │
│      ▼                                    ▼                  │
│  [I₁, I₂, I₃]                    [T₁, T₂, T₃]             │
│                                                              │
│        Cosine Similarity Matrix:                             │
│        ┌─────────────────────────┐                          │
│        │       T₁    T₂    T₃   │                          │
│        │  I₁ [ ✅    ❌    ❌ ] │  Diagonal = MAXIMIZE     │
│        │  I₂ [ ❌    ✅    ❌ ] │  Off-diag  = MINIMIZE    │
│        │  I₃ [ ❌    ❌    ✅ ] │                          │
│        └─────────────────────────┘                          │
│                                                              │
│  → Contrastive Loss pushes matching pairs closer             │
│    and non-matching pairs apart in embedding space            │
└─────────────────────────────────────────────────────────────┘
```

### LLaVA Architecture (Visual Instruction Tuning)

```
  ┌───────────────────────────────────────────────────────────┐
  │                    LLaVA Architecture                      │
  │                                                            │
  │  ┌──────────┐       ┌──────────┐       ┌──────────────┐  │
  │  │          │       │  Linear  │       │              │  │
  │  │  Image   │──────►│Projection│──────►│              │  │
  │  │          │       │  Layer   │       │              │  │
  │  └──────────┘       └──────────┘       │    Large     │  │
  │       │                                │   Language   │  │
  │  ┌────▼─────┐                          │    Model     │──►  Response
  │  │  CLIP    │   Visual Tokens          │  (Vicuna /   │
  │  │  ViT     │──────────────────────────►│   LLaMA)    │
  │  │ Encoder  │                          │              │
  │  └──────────┘                          │              │
  │                                         │              │
  │  ┌──────────────────────┐              │              │
  │  │ "Describe this image"│──────────────►│              │
  │  │  (Text Instruction)  │  Text Tokens │              │
  │  └──────────────────────┘              └──────────────┘
  │                                                            │
  │  Training:                                                 │
  │  Stage 1: Pre-train projection layer (image-text pairs)    │
  │  Stage 2: Fine-tune end-to-end (instruction following)     │
  └───────────────────────────────────────────────────────────┘
```

### Use Cases

- **Zero-Shot Image Classification:** CLIP
- **Visual Question Answering (VQA):** LLaVA, GPT-4V
- **Image Captioning:** BLIP, Flamingo
- **Image Search & Retrieval:** CLIP embeddings
- **Multimodal Chat:** GPT-4V, Gemini

---

## 6. Hybrid and Emerging Architectures

### Overview

Hybrid methodologies integrate multiple distinct generative frameworks to synergistically combine the advantageous characteristics of each constituent model while mitigating their inherent limitations.

### VAE-GAN Hybrid

```
  ┌─────────────────────────────────────────────────────────┐
  │                    VAE-GAN HYBRID                        │
  │                                                          │
  │  ┌──────┐   ┌───────┐   ┌────────┐   ┌──────────────┐ │
  │  │Input │──►│Encoder│──►│Latent z│──►│  Decoder/    │ │
  │  │Image │   │ (VAE) │   │        │   │  Generator   │ │
  │  └──────┘   └───────┘   └────────┘   └──────┬───────┘ │
  │                                              │          │
  │                                              ▼          │
  │             ┌──────────────┐         ┌──────────────┐  │
  │             │  Real Image  │────────►│              │  │
  │             └──────────────┘         │Discriminator │  │
  │                                      │   (GAN)      │  │
  │             ┌──────────────┐         │              │  │
  │             │ Generated    │────────►│  Real/Fake?  │  │
  │             │ Image        │         └──────────────┘  │
  │             └──────────────┘                           │
  │                                                          │
  │  Benefits: VAE's structured latent space                 │
  │          + GAN's sharp, realistic outputs                │
  └─────────────────────────────────────────────────────────┘
```

### Diffusion Transformer (DiT)

```
  ┌─────────────────────────────────────────────────────┐
  │         DIFFUSION TRANSFORMER (DiT)                  │
  │                                                      │
  │  Replaces U-Net with Transformer in Diffusion Model  │
  │                                                      │
  │  ┌──────────────┐                                   │
  │  │ Noisy Latent │                                   │
  │  │    Patches   │                                   │
  │  └──────┬───────┘                                   │
  │         │                                            │
  │         ▼                                            │
  │  ┌──────────────┐                                   │
  │  │ Patchify +   │  (divide into patches/tokens)     │
  │  │ Embed        │                                   │
  │  └──────┬───────┘                                   │
  │         │                                            │
  │         ▼                                            │
  │  ┌──────────────┐   ┌──────────┐                    │
  │  │ Transformer  │◄──│Timestep t│                    │
  │  │ Blocks × N   │◄──│+ Class   │                    │
  │  │(Self-Attn +  │   │Condition │                    │
  │  │ Cross-Attn + │   └──────────┘                    │
  │  │ Feed-Forward)│                                   │
  │  └──────┬───────┘                                   │
  │         │                                            │
  │         ▼                                            │
  │  ┌──────────────┐                                   │
  │  │ Predicted    │                                   │
  │  │ Noise / v    │                                   │
  │  └──────────────┘                                   │
  │                                                      │
  │  Used in: Stable Diffusion 3, Sora, Movie Gen       │
  └─────────────────────────────────────────────────────┘
```

### State-Space Models (Mamba) — Emerging Alternative

```
  ┌─────────────────────────────────────────────────────┐
  │         STATE-SPACE MODELS (SSMs / Mamba)            │
  │                                                      │
  │  Alternative to Transformers for long sequences      │
  │                                                      │
  │  Input ──► [Selective State Space] ──► Output        │
  │                                                      │
  │  • Linear scaling with sequence length (vs O(n²))   │
  │  • Efficient for very long contexts                  │
  │  • Input-dependent selection mechanism               │
  │  • Emerging use in language + vision tasks           │
  └─────────────────────────────────────────────────────┘
```

---

## 7. Architecture Comparison

### At-a-Glance Comparison

```
┌─────────────────┬──────────────┬──────────────┬───────────────┬──────────────┐
│  Architecture   │  Primary     │  Training    │  Output       │  Main Use    │
│                 │  Mechanism   │  Stability   │  Quality      │  Cases       │
├─────────────────┼──────────────┼──────────────┼───────────────┼──────────────┤
│  Transformer    │  Self-       │  High        │  SOTA for     │  NLP, code,  │
│  (GPT/BERT)     │  Attention   │              │  text         │  multimodal  │
├─────────────────┼──────────────┼──────────────┼───────────────┼──────────────┤
│  Diffusion      │  Iterative   │  Very High   │  SOTA for     │  Image/video │
│  Models         │  Denoising   │              │  images       │  generation  │
├─────────────────┼──────────────┼──────────────┼───────────────┼──────────────┤
│  GANs           │  Adversarial │  Variable    │  Sharp,       │  Image syn., │
│                 │  Game        │  (unstable)  │  realistic    │  style xfer  │
├─────────────────┼──────────────┼──────────────┼───────────────┼──────────────┤
│  VAEs           │  Variational │  High        │  Smooth but   │  Anomaly det,│
│                 │  Inference   │              │  blurrier     │  compression │
├─────────────────┼──────────────┼──────────────┼───────────────┼──────────────┤
│  VLMs (CLIP,    │  Contrastive │  High        │  Multimodal   │  Image+Text  │
│   LLaVA)        │  + Attention │              │  understanding│  reasoning   │
├─────────────────┼──────────────┼──────────────┼───────────────┼──────────────┤
│  Hybrids        │  Combined    │  Varies      │  Best of      │  Specialized │
│  (VAE-GAN, DiT) │  methods     │              │  both worlds  │  tasks       │
└─────────────────┴──────────────┴──────────────┴───────────────┴──────────────┘
```

### Evolution Timeline

```
  2014          2017          2018      2020       2022         2024+
   │             │             │         │          │            │
   ├─ GAN        ├─ Transformer├─ BERT   ├─ DDPM    ├─ Stable    ├─ Sora
   ├─ VAE        │             ├─ GPT-1  ├─ GPT-3   │  Diffusion ├─ SD 3
   │             │             │         ├─ CLIP    ├─ DALL-E 2  ├─ GPT-4o
   │             │             │         │          ├─ ChatGPT   ├─ DiT
   │             │             │         │          ├─ Midjourney├─ Gemini
   │             │             │         │          │            ├─ LLaMA 3
   ▼             ▼             ▼         ▼          ▼            ▼
  [Foundation]  [Attention     [Pre-     [Scale     [Consumer    [Multi-
                 Revolution]   training]  Era]       AI Boom]     modal Era]
```

---

## References

1. Vaswani et al., "Attention Is All You Need" (2017) — Transformer architecture
2. Devlin et al., "BERT: Pre-training of Deep Bidirectional Transformers" (2018)
3. Radford et al., GPT series (2018-2023) — OpenAI
4. Ho et al., "Denoising Diffusion Probabilistic Models" (2020)
5. Rombach et al., "High-Resolution Image Synthesis with Latent Diffusion Models" (2021)
6. Goodfellow et al., "Generative Adversarial Networks" (2014)
7. Kingma & Welling, "Auto-Encoding Variational Bayes" (2014)
8. Radford et al., "Learning Transferable Visual Models From Natural Language Supervision" (2021) — CLIP
9. Liu et al., "Visual Instruction Tuning" (2023) — LLaVA
10. Peebles & Xie, "Scalable Diffusion Models with Transformers" (2023) — DiT

---

*Last updated: February 2026*
